import javax.swing.*;
import java.awt.*;

public class Square extends JButton{
    public Square(Color colour){
        setSize(100,100);
        setBackground(colour);
        setVisible(true);
    }
}
